# Most plugins are git-cloned at build time. The only thing COPYed from the
# build context is the optional CDN signer at local/objectfs_cdntoken/ (staged
# always, installed only when --build-arg INCLUDE_CDN_PLUGIN=true). Keep the
# rest of the context out.
charts/
examples/
*.md
.git/
